C Preprocessor defines which affect this driver (see Makefile):
LT_USE_FIFO		- transmit through fifo (doesn't seem to make difference)
LT_USE_OLD_NAMES	- create /dev/ttyLT0 under udev instead of /dev/ttyLTM0
